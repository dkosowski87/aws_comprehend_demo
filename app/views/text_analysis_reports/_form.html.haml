= form_for @text_analysis_report do |f|
  - if @text_analysis_report.errors.any?
    #error_explanation
      %h2= "#{pluralize(@text_analysis_report.errors.count, "error")} prohibited this text_analysis_report from being saved:"
      %ul
        - @text_analysis_report.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
